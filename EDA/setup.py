#!/usr/bin/env python

#this file tells python how the current project should be set up and must be modified accordingly 

from distutils.core import setup

setup(name='project_name',
      version='0.0',
      description='a Tool',
      author='Thomas Hillerton',
      author_email='-',
      url='-',
      packages=['-', '-'],
      py_modules = ['mod1'],
     )

